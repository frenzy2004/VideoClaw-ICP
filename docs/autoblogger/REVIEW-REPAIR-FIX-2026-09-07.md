# Review and repair correction — 7 September 2026

Scope: worker PR #1, branch `automation/persistent-autoblogger-v1`, based on
`bf55435`. No lander, production, schedule or persisted retry-state changes.

## Corrected behavior

1. **Shared repair issue registry.** Independent critique and machine findings
   receive one complete, unique ID registry. Repair and verification receive the
   same registry; the verifier's output schema constrains IDs to that inventory.
   Missing, unknown, duplicate or unresolved evaluations cannot grant approval.
   Every replacement binding still receives a fresh support review.
2. **Ordinary editorial references.** A bounded, adjacent-sentence grammar can
   resolve an explicitly hypothetical example or worksheet as the subject of an
   explanation. It does not exempt arbitrary predicates, product antecedents,
   added capability clauses or ambiguous software references. This is reference
   resolution, not evidence of factual support.
3. **Private campaign context.** `customerTrigger` is copied verbatim from the
   configured `candidate.icp`, constrained during generation/repair and checked
   again in code. It is not a source claim and may not have a claim binding.
   Public metadata, prose, graphics, FAQs and `competitorGap` retain their evidence
   checks. No lander frontmatter schema was copied or changed. Old generation
   responses with `/customerTrigger` bindings now fail the internal DTO contract;
  saved receipts are not silently migrated or rewritten.
4. **Topical source selection and allocation.** Retrieved bodies are screened for
   the query's core terms and qualifiers; format words such as `checklist` alone
   cannot qualify a source. The supplied URL pool is ranked before selecting at
   most four distinct page identities, including an authority. Body-only anchors
   guide composition while preserving complete source qualifications. New
   extraction records a validated `bodyStart` offset through to the source facts;
   a retained H2–H6 heading cannot make unrelated prose qualify. Legacy facts
   without that boundary retain only the limited lexical screen, not an inferred
   heading/body distinction. This is a
   conservative English lexical screen, not semantic verification or proof of
   demand. Differently worded relevant sources can be missed.

The initial reviewed derivation target is 120 words per page, leaving 60 words of
margin before the unchanged final 180-word ceiling. Exceeding the initial target
routes the one bounded repair to whole-article restructuring. Potential exposure
counts include contextual citations for original guidance; they are diagnostics,
not copying evidence or a hard limit on original advice. The final independent
review and source ceiling remain mandatory. Native formatting findings are also
collected when source allocation needs repair, so one problem does not conceal
another from the single repair pass.

## Verification evidence

Final repository verification: **1,332 tests across 50 files passed**, followed
by lint, typecheck with generated route types, build and whitespace checks.
Existing gray-matter direct-eval and vinext mixed-import build warnings remain;
no warning-free build is claimed. Changed-file credential-value checks found no
leak, and ignored runtime files and raw artifacts were not staged.

Regressions were demonstrated failing before their respective fixes. Tests cover
the issue registry, context consistency, ordinary-reference positive/negative
controls, topical selection, page aliases, source allocations and retained hard
limits. Independent review additionally identified heading/body contamination
and the `does` stopword normalization defect. RED-first regressions now cover
H2–H6 boundaries, preserved qualifications, offset validation/propagation and
`does`/`has` normalization.

Final independent re-review cleared both findings, reran 538 focused tests and
verified heading rejection, equal `does`/`do` behavior, UTF-16 boundary propagation
and combined formatting/allocation repair. No remaining findings were reported
within this patch scope; this is not a repository-wide security audit.

The offline end-to-end fixture exercises 50 candidates, ten deep checks, three
drafts and bounded review/repair with synthetic provider responses, real local
state, Markdown/SVG artifacts and the native lander parser/fixture renderer. The
one-article pending-metrics pilot, idempotent rerun and rejected-support controls
are also covered. These are software tests, not live keyword or editorial proof.

Separately, a generated synthetic bundle passed the **unmodified** lander
`check:blog` (32 tests), lint and full Next build in a disposable checkout of
`seo/founder-video-blog-launch` at
`b6b0833c78443b44b12bf6d33f05baa7ac8427d3`. Locked dependency installation,
workspace-integrity checks and cleanup passed. No original lander file changed.
Bundle SHA-256:
`2a790abdb12f454fabf047d394416c7fcc8aaf96188e65ecf497aab21dfbd403`.
The local ignored report is
`artifacts/autoblogger/review-repair-native-qa-2026-09-07/validation-report.json`.
This proves contract/build compatibility, not a fresh live article or visual QA.

## Saved failure and remaining milestone

The fifth live run remains failed. Hash-verified saved review classifications
still account for 136, 66, **226**, and 0 derived words across its four sources;
226 still fails the unchanged 180-word gate. Its old metadata-binding format is
rejected rather than accepted under the new contract. The new topical screen
does not admit that saved source set as sufficient authoritative topic evidence.

A separate in-memory diagnostic changed only the private metadata field and
removed its obsolete binding; public wording stayed byte-identical. The ordinary
reference regression cleared and deterministic findings were empty. That
diagnostic did not generate an artifact, change a receipt, rerun an independent
critic or clear the 226-word source overuse. It isolates the checker correction;
it is not a successful replay of the old run under a new label.

No paid API call, new live draft, retry grant, state reset, generated lander PR,
publication or schedule activation occurred in this correction. Candidate retry
history remains terminal at five. State SHA-256 is unchanged:
`970ce9af34e53ee2d3ec0e9f6ef42da61698be66b237ba7bbdf032ef25ffb5a9`.

The engineering milestone still requires a **fresh, separately recorded live
artifact-only run** to pass automatic research, drafting, critique/repair and
native QA without manual evidence injection or rewriting. Do not describe these
offline fixes, fixture artifacts or historical assisted results as that success.
Do not erase the exhausted candidate's history to obtain another attempt.
