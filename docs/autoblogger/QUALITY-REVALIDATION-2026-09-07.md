# Artifact-only composition revalidation

Scope: worker PR #1 on `automation/persistent-autoblogger-v1`, starting at
`7d083db`. The original lander, PR #55, production branches, publication and
schedule activation remain outside this change.

## Correction being tested

The eighth attempt successfully researched the topic, but its repair retained a
long sequence of publisher summaries. Three pages exceeded the unchanged
180-word source-derivation ceiling, and one passage was copied. Parser fixes
alone could not make that article acceptable.

The writer now starts with a usable decision worksheet, a visibly hypothetical
worked example and troubleshooting choices, aiming at a focused 800–1,100-word
guide. It uses brief, non-repeated source notes where they materially help the
reader. The repair receives the same composition strategy when cumulative
source-use issues occur. Relevant contextual bindings are still required;
calling a paraphrase original does not exempt it from independent review.

The source inventory, copying detection, 180-word ceiling, complete binding
review, fixed candidate, exact FAQ questions and native lander checks are
unchanged. No article prose or evidence was manually injected. Prompt inspection
and passing software tests are not proof that live generation has improved.

## Explicit one-use boundary

The latest user instruction authorizes one local quality revalidation after
these fixes, through `--approve-quality-revalidation`. It requires the exact
failed eighth run, the unchanged candidate and retained approval history, and
the audited state SHA-256:

`2a73391994794137f9d0e9cc388f4ac0d1a26218c740bfacda8176bd11afbf96`.

The prior report and closed audit must prove complete research and all four
HTTP-200 model stages, with final copy rejection and no accepted artifact,
native validation or generated PR. Their hashes and the implementation hash are
checked again before state persistence and each paid request. The old consumed
grant is retained and hashed. The new grant permits attempt nine once, not a
state reset, tenth attempt, ordinary retry-cap increase or scheduled execution.

Targeted regressions cover one-use consumption, exact candidate/run identity,
changed receipts or code, incomplete evidence, altered history, replay,
schedule rejection and refusal of a tenth attempt. Independent scoped review
found no actionable blocker in the prompt or one-use guard changes.

## Pre-run verification

All **1,632 tests across 51 files**, lint, regenerated-route typecheck, build and
whitespace checks passed. Existing gray-matter eval and vinext mixed-import
warnings remain. The original lander was clean at
`b6b0833c78443b44b12bf6d33f05baa7ac8427d3`; PR #55 was open/unmerged and
`AUTOBLOG_SCHEDULE_ENABLED` was `false`.

## Live result

Run `quality-revalidation-product-demo-pilot-2026-09-07` **failed**. It ran from
15:50:55.485 to 16:10:45.800 UTC on September 7 (23:50 to 00:10 Malaysia time).
Fresh research succeeded with eight exact-query organic results, three selected
observed FAQ questions and four automatically retrieved source bodies. No manual
evidence injection was used.

| Observation | Apify run | Dataset |
| --- | --- | --- |
| Autocomplete | `ENFMHz57L9RuFpJJz` | `5KiJvWlgbSDjTYUK3` |
| Exact US/en organic SERP | `YFK5epTUB4xtbWpC5` | `raem48hXoaM1dUhDQ` |
| PAA | `9AgyCepGqFyszdCzM` | `NOQgi3U9lcEtpLEGs` |
| Four supporting source queries | `nO9MeNv12bS0vfzRb` | `SIitwwkMwEghm5iR6` |

Draft generation started at 15:51:53.922 UTC. The local request eventually failed
with `HTTP request timed out after 240000ms`. There is one retained request and
**no returned model response, response ID or usage receipt**. Provider completion
and billing are unknown, not zero. No critique, repair, accepted Markdown/SVG,
native article QA or generated lander PR occurred.

### Local interruption evidence

A scoped read-only check of the macOS power log recorded lid-closed sleep at
23:52:44 Malaysia time, about 51 seconds after drafting started. Further sleep
followed at 23:53:24; the next maintenance wake was at 00:10:42, immediately before
the timeout receipt. This establishes a local sleep interruption overlapping the
request. It does **not** prove that the provider itself was slow or that the new
composition passed or failed editorial review. The timeout was not increased and
no model fallback or automatic retry was introduced.

The request used the existing `store: false` contract. There is no retained response
ID to retrieve. Switching to background mode is not an invisible recovery: it
uses a retrievable response object and temporary storage for polling; retention
depends on account settings and the explicit storage option. See the official
[background-mode constraints](https://developers.openai.com/api/docs/guides/background).
That change was not made, and the interrupted request was not repeated.

### Preserved state and next boundary

Post-run state SHA-256:
`5976d25d5ec7fa67578342d3da8aedec77b481f03d21044d22be972ad2ef9a33`.
All 13 runs and 25 failure records remain; `manualPilot` is null and no generated
content hash was added. The candidate is terminal at nine. The consumed grant
cannot be replayed, and there is no tenth-attempt path.

The software corrections have passing tests, but the clean live milestone is
still unproven. Any separately authorized continuation must preserve this failed
receipt and run on an awake, connected local machine or an approved unattended
runner. A local process cannot continue reliably through lid-closed sleep. Scoped
Actions access remains a separate team dependency; do not substitute broader
credentials or activate the schedule to work around it. No additional keys are
needed for the existing local providers.

## Final checkpoint

After the interrupted run, all **1,632 tests across 51 files**, lint,
regenerated-route typecheck, build and whitespace checks passed again on
September 8 Malaysia time. Independent read-only review confirmed the failed
receipts, retained state and sleep timing; it found no demonstrated timeout-handler
defect. Scanning all nine changed/new files against both runtime credential values
found zero leaks. Raw evidence, model inputs, state and credential files remain
private and ignored, not part of the PR.
