# Alternative-topic artifact-only pilot — 6 September 2026

**Outcome: the single live pilot ran and failed its fresh organic-evidence gate.
No model call, generated article or native generated-article QA pass occurred.**

## Scope

The user authorized researching five alternative topics and selecting one genuinely
different, evidence-qualified opportunity for a single automatic local pilot.
The retained `vc-c2-001` candidate, its three failures and its unused fourth-attempt
approval must remain unchanged. This is not a retry reset or an article approval.

The milestone is one unassisted run through live research, automatic source-body
verification, structured drafting, independent critique, at most one repair,
independent verification and the lander's native checks. Model outputs and source
bodies remain in private ignored receipts. Only compact provenance and the outcome
belong in this report.

## Five-topic diagnostic

Initial scan completed at 21:16 UTC on 5 September. These were research-only calls:
zero worker reservations, zero model calls, and the retained state hash was unchanged.

| Candidate query | Initial organic results | Initial finding |
| --- | ---: | --- |
| `product demo checklist` | 0 | Autocomplete exists; empty organic scraper response requires investigation, not a zero-demand claim. |
| `backup product demo` | 8 | Results and questions lean toward backup software; unsuitable for a founder's fallback presentation workflow. |
| `product demo backup checklist` | 9 | Backup/disaster-recovery intent appears in the results; no qualifying FAQ evidence. |
| `startup demo video` | 0 | Autocomplete exists; empty organic scraper response. |
| `product demo video script` | 0 | Autocomplete exists; empty organic scraper response. |

Exact initial provider provenance:

- Autocomplete: `K0oGIvjemW1Lg9L6q` / `ip9VHLfThbQeLtb4b`.
- US/en desktop first-page organic SERPs: `ojURkTm9BSc7DUYB7` / `FR6KNoEU5Pw7p3wS2`.
- PAA attempts: `B9V7fKawjxbtUaahF` / `sYhR43C1ysoJ6Pygv`, then
  `yTMLKDltxMEvvqJZ0` / `RbYSio7yMYipBAJou`.

The empty organic arrays were present in the original provider rows; normalization
did not discard populated results. None of this establishes search volume, keyword
difficulty or CPC. All three metrics remain provider-pending.

One exact-query organic retry recovered eight results for `startup demo video`,
but its subsequent question checks returned no qualifying PAA. The worker now
retries zero-organic rows once per scan, retains both completed attempt IDs, and
binds each selected row to the observation that actually supplied it. It does not
change query, locale, page, device or evidence gates. Persistent empty results are
not treated as proof of zero demand.

The two finalists with qualifying questions were rechecked with the production
researcher. `product demo checklist` passed the manual-pilot evidence gate at
21:26 UTC: three organic results, twelve observed PAA questions, three selected
relevant FAQs and four automatically retrieved body documents (15 context groups).
The script topic still returned no organic evidence and was not selected.

- Selected article ID: `vc-c1-d-ab0e55b1dd7f78fa`.
- Campaign: newly funded founder preparing a customer-facing product demonstration.
- Candidate title: **Product Demo Checklist: Plan, Record and Rehearse**.
- Selected organic run/dataset: `EDlGglUyl2kSfiLJa` / `fGUQbXhrgAPHMbgbs`.
- Selected PAA run/dataset: `Ggkt44GuSp8uePH05` / `ia7aMXhX3kqbiNzxe`.
- Automatically fetched sources: a LinkedIn demo-preparation post, Y Combinator's
  Demo Day pitch guide, and YC's Demo Gorilla and Storylane company pages.

Selection diagnostics did not reserve a candidate or make model calls. Retrieved
sources establish what those publishers said, not the truth of every claim or a
proven competitive gap. The actual pilot must freshly collect its own evidence;
these diagnostic source facts are not injected into its draft.

## Actual worker run

Run `automated-product-demo-pilot-2026-09-06` started at 21:36 UTC on 5 September.
Fresh GET-only GitHub inventory matched the clean local lander at
`b6b0833c78443b44b12bf6d33f05baa7ac8427d3`; PR #55 was open/unmerged. The selected
candidate was reserved at attempt **1**, not under the old fourth-attempt grant.

| Stage | Observed outcome |
| --- | --- |
| Automatic autocomplete | Completed; `PplnGwag289rlSIaw` / `LZuKdttN5qUskAT5g` |
| Fresh organic request | Zero results; `I7dmI0ChPwlGqYnRq` / `8egVsLEccz46DWFun` |
| One bounded organic retry | Zero results again; `Lo6OCmhR787IvfCvb` / `JXaK4hWcxahnf7hvj` |
| Deep source checks | Not reached |
| GPT-5.5 draft / critique / repair | Not reached; **zero calls** |
| Markdown / SVG bundle | None generated |
| Native generated-article QA | Not reached |
| Lander PR / publication / deployment | None |

The earlier selection snapshot had three organic results. The actual worker did
not substitute that snapshot, relabel its timestamp or inject its checked source
facts after fresh collection failed. This proves the safety stop, **not** the
engineering milestone or the absence of search demand.

## Fixes and retained state

- Zero-organic collection gets one exact-query retry, with both completed
  attempts retained and no query/locale/gate changes.
- A narrow explicit target switch parks the old unused grant unchanged. It is
  bound to one distinct candidate and one manual artifact-only run; it cannot
  publish, activate scheduling, consume the old grant or reset normal retry caps.
- Actual-state review caught an old matrix/queue identity collision. Only the
  old transient backlog representation is reconciled against all six identities
  in the retained grant. Stored old decisions and history are not rewritten.
- The failed live report originally said `retryable: true` although its one-use
  switch was consumed. Failure classification and reports now reflect the real
  terminal state. Five regression cases were reproduced failing before repair.
- A separate, audited compare-and-swap correction marks only the new decision
  terminal at attempt **1**. The original failed report and failure entries remain
  intact; a classification-correction entry explains the change. No retry or
  model call was made by that correction.
- The old `vc-c2-001` remains terminal at attempt **3**. Its approval still has
  `consumedAt: null`; old runs, failures, decisions and grant values were preserved.
- Private runtime artifacts are excluded from application lint/typecheck inputs,
  while strict checking remains enabled for source and tests.

Current blocker: reliable fresh organic collection for the chosen query. The
provider can report a successful job containing an empty result array; there is
no reliable error marker in the examined rows to distinguish genuine emptiness
from incomplete collection. The next investigation should compare retained raw
HTML with extracted results before another explicitly authorized live attempt.
It is not a missing OpenAI or Apify credential, and repeatedly minting run IDs is
not an acceptable fix.

## Software verification

- **986 tests / 49 files passed** with `npm test -- --maxWorkers=4`.
- Typecheck, lint, build and whitespace checks passed. Existing dependency build
  warnings remain; this is not a warning-free build claim.
- The first default-parallel run timed out in the existing 250-row content-map
  test. The unchanged isolated test and full four-worker suite passed; no test
  timeout or assertion was relaxed.
- Independent review found and rechecked the old transient-backlog collision,
  and reported no remaining P1/P2 in the scoped code patch. The five final
  retry-classification regressions were observed failing before the correction.
- Actual runtime secrets and secret patterns were absent from the proposed patch
  and this report. Private candidate, research, state and model-replay paths stay
  excluded from Git.

These checks verify the software changes and fixture paths, not a successful new
live article. See the [verification record](VERIFICATION.md) for the distinction.

## Release boundary

Only [worker PR #1](https://github.com/frenzy2004/VideoClaw-ICP/pull/1) is in scope.
The original lander checkout, PR #55, production, article approval flags and the
disabled schedule are not changed. A passing local artifact would still require
human review and would not authorize a lander PR or publishing.
