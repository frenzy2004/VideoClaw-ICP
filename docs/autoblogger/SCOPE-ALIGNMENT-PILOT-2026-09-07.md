# Fixed-title evidence alignment and artifact-only verification

Scope: worker PR #1, `automation/persistent-autoblogger-v1`, starting at
`6a8f587`. The original lander, PR #55, production, publishing and scheduled
execution remain outside this change.

## Failure addressed

The sixth live attempt correctly failed independent review: the fixed title
promised recording instructions while its sources and body focused on a live
sales demonstration. The repair instructions were corrected after that run,
but initial research and composition still did not consistently cover the title.
The failed receipts remain unchanged; they are not a successful article.

## Correction

- Body research always performs one bounded supporting search, with four short
  topic queries: one per scoped publisher. Format/framing words may be removed
  for supporting-source discovery only; the primary US/en SERP query remains
  exact. Substantive topic qualifiers remain. This replaces the initial
  two-query, full-title/OR-list approach after its live failure described below.
- The existing 24-source-fetch allowance is shared across organic and all four
  support result sets. Exact query, US/en, desktop, first-page, URL, fetched-body
  and source relevance checks still apply. Support results retain their own
  provenance and never count as keyword organic results or demand metrics.
- Final source selection preserves complementary title-task passages among
  topic-qualified bodies, reserving an authoritative source and up to four pages.
  A higher count of keyword mentions alone cannot evict the recording/rehearsal
  sources. This lexical diversity ranking is not semantic verification; the
  independent critic must still establish actual scope and citation support.
- Source planning carries the fixed title alongside the primary keyword. The
  initial draft, critique, repair and verification all receive the same immutable
  title constraint. Missing promised coverage is still a blocking editorial issue.
- Draft instructions separate source-attributed summaries from original
  worksheets, avoiding an adjacent citation that appears to endorse unsupported
  additions. No source facts, generated copy or review decisions are hand-edited.

Six targeted failures were reproduced before their fixes: lost title query,
skipped title discovery, incorrect support query, absent title in source planning,
title sources discarded by the fetch limit, and practical sources evicted by
keyword-heavy program pages at final selection. Live generation is the separate
test of prompt effectiveness; unit tests alone do not prove article quality.

## Verification boundary

The user's renewed instruction authorizes one new local artifact-only test after
the fixes. The explicit scope-alignment retry must refer to the same candidate's
failed sixth run, preserve its consumed approval chain, and end after attempt
seven. It is not a reset, ordinary retry-cap increase or scheduling permission.

The test must collect new evidence automatically, use the configured GPT-5.5
without model fallback, allow no more than four model requests, retain one bounded
repair, and pass the unchanged lander checks in a disposable checkout before any
review artifact is accepted. No generated lander PR or publishing capability is
provided to this local runner.

## Pre-run verification

Before attempt seven, all 1,451 tests across 51 files, lint, regenerated-route typecheck and build passed.
Existing gray-matter eval and vinext mixed-import build warnings remain. The
50-candidate offline integration exercised the initial two-query deep searches,
separately from organic keyword requests, and still validates three fixture
artifacts with the per-ICP cap and idempotent state behavior.

Independent review found the final-selection eviction described above; its
regression failed before the fix. Re-review confirmed practical recording and
rehearsal sources survive the four-source cap, authority is retained, heading-only
and unrelated bodies do not qualify, and FAQ/organic/provenance behavior remains
unchanged. The explicit seventh-attempt guards also passed scoped review.

Runtime-key-value scanning found zero credential leaks in the 18 changed/new files
at that checkpoint. Before live execution, the retained state still had SHA-256
`d4b778b9e39880b9cec288a78c46aa1a809c196bbc7be836b69e15df821b64b7`.

## Seventh attempt: source-stage failure, no model requests

`scope-aligned-product-demo-pilot-2026-09-07` ran from 14:29:28 to 14:30:07 UTC.
It collected eight organic results and ten question strings, retaining the three
relevant product-demo FAQs. The source stage failed before drafting: no selected
authoritative body passed alongside the otherwise usable organic sources.
No model stage, accepted bundle, native article QA or generated PR occurred.
The closed execution audit and failed run report are retained unchanged.

Read-only inspection of the actual support dataset showed that the long full-title
and OR-domain queries returned unrelated training pages, job pages and Hacker News
threads. The source gate correctly rejected those results. Independently reading
the organic URLs found several usable practitioner pages, but none met the
configured authority policy. The authority policy was not broadened to force a pass.

| Observation | Run | Dataset |
| --- | --- | --- |
| Autocomplete | `tt6TqP8hIT8ZrLIKT` | `gLuKjQHu8WEzGcsWX` |
| Exact US/en organic SERP | `374Asmh0Wze6TP4ee` | `DctW1IaYNE7NzQlLY` |
| PAA | `sM8ULfffY9UbayQOU` | `aqUOhYDNpfcfpnD15` |
| Unhelpful support queries | `JbZfPJdO0gd7splbS` | `0NBotZ7k4sZsIIjuR` |

Post-run state SHA-256:
`9f0a86172909980567e0e27910baf060a0a7f1a796153880e0b91d256dff8316`.

## Short-query diagnostic

A failing regression preceded the four-query correction. The subsequent live
source-only diagnostic, run `HNzNwj2B8sa2hso75`, dataset `jAEc7nssJzBvT5f7S`,
returned directly relevant guides. The existing production source checker
successfully retrieved four Descript article bodies, including product-demo
recording instructions. URLs, fetch times and body hashes are in the ignored
`short-query-source-diagnostic-2026-09-07/validation-report.json` artifact.

This diagnostic spent on source searches only, made no model calls, changed no
pilot state, and is not a clean worker pass. Its page facts are not manually
inserted into any candidate or the next live attempt. The current query fixtures
exercise forty supporting queries for ten deep candidates, separately from the
fifty original keyword searches; 88 targeted tests passed after the correction.

## Bounded engineering continuation

The current instruction to finish the working artifact-only pipeline is recorded
through `--approve-engineering-resume`, not an automatic retry loop. The generic
resume reason has an explicit maximum attempt of eight and requires the exact
consumed seventh grant and unchanged candidate identity. There is no ninth path.

Before granting it, the local runner reads the ignored failed run report and its
closed execution audit. They must agree on one scanned candidate, source-stage
failure, zero drafted/validated/PR counts, and the complete five-event source-only
sequence with no model or native stage. Missing model files alone are insufficient.
The report, audit and implementation hashes are retained in the grant; the files
and code hash are rechecked before state persistence and paid execution. All prior
failures and approvals remain unchanged and consumption is atomic at reservation.

Substantive source-query tokens also remain intact: separate regressions protect
SaaS, Kubernetes, Windows and C++ from the stemming previously used for lexical
relevance scoring. That scoring helper is not reused to emit search queries.

Independent review cleared the exact eighth-attempt guards before execution. The
complete worker suite passed 1,522 tests across 51 files, lint, typecheck and build
at that pre-run checkpoint. Those checks are not the live outcome below.

## Eighth attempt: research works; the repaired copy is rejected

`engineering-resume-product-demo-pilot-2026-09-07` ran from 14:50:06 to 14:56:48 UTC.
It automatically collected eight exact-query organic results, three relevant
observed FAQs, and four source bodies: three Descript guides and the YC Rivia.AI
company page. Plan, Record and Rehearse now have substantive body coverage.
No manual evidence injection or article rewriting was used.

| Observation | Run | Dataset |
| --- | --- | --- |
| Autocomplete | `ORnoVSmIV98EIiU46` | `aaiY8dYHRxGC2RnKa` |
| Exact US/en organic SERP | `3EAN6iu7RlefgSh0e` | `OeX7lZxkI47h60x77` |
| PAA | `r7YcCEPy9L0qcJTKE` | `9CZb2YwXcxM3ro1y4` |
| Four short supporting queries | `FMZJ2BL2y2K2kaTeR` | `PiIroiTW5cMI5r4FI` |

All four HTTP-200 GPT-5.5 requests completed: generation, independent critique,
one repair, and independent verification. Their reported total usage was 199,918
tokens. The configured model resolved to `gpt-5.5-2026-04-23`; no fallback occurred.

The final worker result is **failed**, with zero accepted Markdown bundles, zero
native article validations and zero generated lander PRs. The final safety report
recorded one copied passage, 22 binding findings and seven overlapping source-budget
findings. Independent read-only review separated real copy defects from parser errors:

- One repaired sentence repeats a source passage rather than paraphrasing it.
- Sources 1–3 account for **221, 214 and 326 reviewed derived words**, each above
  the unchanged 180-word per-page ceiling. These are three overused sources, not
  seven distinct ones. Source 4 accounts for 62 words.
- Eighteen binding findings come from splitting dotted names such as Rivia.AI or
  mishandling punctuation followed by closing quotes. Four are ordinary-reference
  false positives. Neither class of checker error excuses the genuine copy defects.

The model repair did receive the initial source overages, contributing binding
indices and a whole-article restructuring instruction. This was not missing source
evidence. It nevertheless preserved repeated source summaries. A crowded repair
input is a plausible contributing factor, not a proven causal explanation.

Post-run state SHA-256:
`2a73391994794137f9d0e9cc388f4ac0d1a26218c740bfacda8176bd11afbf96`.
The candidate is terminal at eight, with every earlier failure preserved and no
ninth-attempt path. No successful-pilot marker or generated article hash was added.

## Follow-up software corrections: offline only

- Aggregate source-budget and planning-allocation issues now appear as explicit
  article-level repair requirements, with the required reduction to the 120-word
  planning target. They are not treated as a patch to the last contributing sentence.
- A page already above the final limit no longer hides a different page's planning
  warning. In this receipt, source 2's initial 144 words must still flag its exhausted
  review reserve even while sources 1 and 3 exceed 180. Invalid review ledgers and
  genuinely original guidance do not acquire fabricated derivation counts.
- Localized targets carry fact IDs referencing the complete source inventory once,
  instead of repeating full source bodies for every finding. The inventory and
  qualifiers remain unchanged. The repair and verifier both receive the lookup rule.
- Dotted-name and quoted-sentence segmentation is corrected with regression tests;
  actual unsupported product references and missing bindings must still fail.

An offline request comparison using the unchanged saved model responses reduced the
repair input from 260,094 to 176,379 bytes at that checkpoint, and localized targets
from 103,863 to 32,436 bytes. The source inventory was byte-identical. This is prompt
input verification, **not proof of improved live generation**. Later reference
corrections can reduce the target set further.

The unchanged repaired article must remain rejected for copying and source overuse.
Old verification IDs also cannot approve a newly changed issue registry. No model
responses, source facts, article prose or failed run reports have been edited, and
there has been no ninth live call sequence, native QA bypass or state reset.

## Final branch verification

Fresh complete checks passed: **1,591 tests across 51 files**, repository lint,
typecheck with regenerated route types, build and whitespace checks. Existing
gray-matter eval and vinext mixed-import warnings remain; this is not a warning-free
build. The 50-candidate fixture still exercises ten deep checks, three artifact
outputs, the per-ICP cap and restart/idempotency behavior. Fixture success is not
native QA of this rejected live article.

Read-only inspection of the exact hash-verified eighth-run context, repaired draft
and final review now finds **zero binding errors**, versus 22 during the live run.
The one copied passage remains blocked, and independently classified source-use
totals remain 221/214/326/62. The final independent model verdict remains rejected;
no receipt or article text was rewritten. Narrow ordinary-reference regressions
also retain negative tests for actual unapproved product capabilities.

Independent review caught concatenated-sentence and later-pronoun bypasses in the
first tokenizer patch. Five behavioral regressions preceded the correction; an
ordinary first reference can no longer exempt a later capability assertion.
Re-review cleared 38 positive/negative probes, including exact approved claims and
tampered bindings. Punctuation inside a bound pronoun span remains conservatively
ambiguous; this checker is not a general natural-language reference resolver.
No important findings remain in the scoped source/repair and checker reviews.

The original lander stays clean at `b6b0833`, PR #55 stays open/unmerged, and
`AUTOBLOG_SCHEDULE_ENABLED` remains `false`. Local state retains the post-run hash
above. The remaining milestone is generated-copy quality plus live native QA—not
another request for the already working local API keys.
