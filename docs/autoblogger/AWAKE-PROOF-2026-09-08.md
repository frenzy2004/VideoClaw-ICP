# Awake live attempt and unchanged-article revalidation

Scope: worker PR #1, branch `automation/persistent-autoblogger-v1`, starting at
`802973f`. No production branch, original lander source, PR #55, deployment,
publishing, indexing or schedule activation is part of this work.

## Authority and preserved history

The user explicitly requested **“continue prove it”** after the ninth attempt
was interrupted by lid-closed sleep. The new `--approve-interruption-resume`
entry point authorizes one tenth attempt against that exact closed failure;
the old quality flag cannot authorize it. There is no eleventh-attempt path.
The ordinary candidate cap remains three.

Pre-run retained state SHA-256:
`5976d25d5ec7fa67578342d3da8aedec77b481f03d21044d22be972ad2ef9a33`.

The existing approval structure is reused with a discriminated prior-run proof.
The prior report must show the exact first-request timeout, one researched
candidate and zero drafts, native validations or generated PRs. The ordered
closed audit must contain source research, the first model-stage start and the
failure, without a returned model response or later stage. Candidate identity,
receipt hashes and implementation hashes are rechecked before paid work. Prior
consumed approvals and failures remain intact; no history was reset.

New regression tests first failed for the missing CLI/proof path, then passed.
They cover explicit authority, malformed/intervening stages, no replay or
scheduled use, and successful final state persistence without invalidating the
retained historical approvals. Independent scoped review found no actionable
blocker before execution.

## Execution controls

The local test runs under a process-bound macOS `caffeinate -i -s -t 1800`
assertion. This prevents idle sleep while supported by the OS, but does not
override lid-closed sleep or guarantee network/power availability. It does not
change permanent power settings. The Mac was awake with usable battery power at
preflight.

The candidate remains **Product Demo Checklist: Plan, Record and Rehearse**,
targeting `product demo checklist`. Research is recollected automatically;
no old source facts, article rewriting or manual evidence is injected. GPT-5.5,
four maximum model calls, one repair, source-use/copying checks, and native lander
validation remain unchanged. Paid keyword metrics remain pending, so output can
only be a review artifact, never a generated lander PR.

## Pre-run software verification

All **1,644 tests across 51 files** and lint passed. A test-only event-array type
annotation was corrected, then regenerated-route typecheck and build passed.
The final narrow state guard also passed **411 focused tests**. Existing
dependency build warnings remain. Fresh read-only preflight verified the original
lander clean at `b6b0833c78443b44b12bf6d33f05baa7ac8427d3`, PR #55 open/unmerged,
and `AUTOBLOG_SCHEDULE_ENABLED=false`.

## Live result

Run ID: `awake-product-demo-proof-2026-09-08`. The original live worker run is
**failed**, not an uninterrupted success. It completed automatic research and all
four GPT-5.5 requests, but five deterministic reference-resolution false positives
stopped it before native validation. Its closed report, state and receipts remain
unchanged. There is no eleventh-attempt path or new paid continuation.

The run started at `2026-09-07T22:42:02.687Z` and ended at
`2026-09-07T22:47:21.664Z` (8 September Malaysia date). It collected eight organic
results, nine observed PAA strings, three selected relevant questions and four
retrieved source bodies. Exact Apify provenance:

| Observation | Run | Dataset |
| --- | --- | --- |
| Autocomplete | `BP9T1TKQP0qJwhZOb` | `Rg99MSz46heStaLhr` |
| US/en exact SERP | `uCQhs2c9Whfifzfb1` | `rPd3JYgbkPblabmly` |
| PAA supplement | `oOC9AqgpY3NZDXe1B` | `FB3DZXx5daDcx2bsc` |

All four model calls returned HTTP 200, resolving the configured `gpt-5.5` to
`gpt-5.5-2026-04-23`. Total returned usage: **147,990 tokens**, comprising
17,009 draft, 35,906 critique, 47,174 repair and 47,901 final verification.
This is reported API usage, not a dollar-cost estimate. No model fallback occurred.

The initial critic approved the draft; automated checks triggered the single repair.
Final independent verification approved the repaired article, with all 116 current
binding hashes matching and no unsupported bindings or new issues. Independent
code-side review confirmed that the five remaining checker findings referred to
a worksheet, company history, an opening, and a publisher-attributed generic
demonstration—not VideoClaw capabilities. They were not waived as factual claims.

## Post-fix proof boundary

Regression tests reproduced the false positives before implementation. The fixes
resolve bounded parallel worksheet references, explicit human revision objects
and source-identified recording instructions. Unsupported capabilities, software
subjects, missing antecedents and appended claims remain rejected. All 464
content-bundle tests passed after the correction. Independent review then caught
first-label hostname matching that admitted publisher lookalikes. Exact trusted
host matching fixed it, and **467 content-bundle tests** passed, including both
lookalike-domain regressions. Final independent scoped review found no blockers.

Revalidation uses the exact saved research, repaired model response, original
repair-issue registry and independent reviewer response. The live drafter and
diagnostic share the same final content, support, repair-resolution, source-use
and materialization checks. No article sentence, source fact or review verdict is
edited, no model call is made, and the worker state is not reset. A separate
`post_fix_revalidation` receipt must not be mistaken for a successful live run.

Pinned input hashes:

| Receipt | Payload SHA-256 |
| --- | --- |
| Drafting context | `7a659a53f9cd6c8ee22ddb0720b908cb0822de0eeb79af19cc9bb693f99ad4d4` |
| Repaired response | `3d4c2536bc181fb74f208322fc4b4fdcacca34065487b3e890c868a4573654c1` |
| Final reviewer response | `00588e3c976119f8c94aaa1ca55309e136a325f51fc0af7edec6c076ce0a7113` |

Retained terminal attempt-ten state SHA-256:
`2cd487b55e88b6eae1ae358d68f5d1be985bb143a01eb3c9d55fff2fc70a3619`.

## Native validation

The unchanged generated bundle passed **32 native lander blog tests, lint and a
full Next build**, plus checkout integrity and cleanup, in a disposable clone at
`b6b0833`. Bundle SHA-256:
`15a7381b75c317a9ec678fc6c5bb88328d40aae8b6900505c77a271e0a21cd1f`.

The saved independent verdict classified 100 CloudShare-derived words and 78
Descript software-demo-derived words; the two other source pages had zero
classified derived words. All are below the unchanged 180-word per-page ceiling.
These are model-reviewed classifications, not a guarantee of originality.

Private local outputs are in
`artifacts/autoblogger/awake-product-demo-postfix-2026-09-08/`: Markdown, SVG,
hash-verified materialized bundle, native-validation report and explicit
`post_fix_revalidation` outcome. They remain Git-ignored. The original live report
and terminal state hashes were rechecked unchanged after native validation.

Final worker checks passed **1,703 tests across 51 files**, lint, regenerated-route
typecheck, build and whitespace checks. Existing dependency build warnings remain.

A separate disposable review checkout serves the same file at
`http://127.0.0.1:3002/blog/product-demo-checklist`. Browser checks found no page
overflow at 390px or 1280px, no console errors/warnings or framework error overlay,
one H1, a 3px keyboard-focus outline, loaded editorial SVG, and working product
video play/pause controls with `playsinline` and no autoplay. The page retains
`noindex, nofollow` and the canonical `https://videoclaw.com/blog/product-demo-checklist`.
Home, blog index, the generated article, all three related guides, `/download`,
the SVG and poster returned HTTP 200. Preview `robots.txt` disallows all crawling,
the sitemap is empty, and `llms.txt` lists no unpublished guides. These are local
preview checks, not verification of production indexing or Lighthouse scores.

No original lander files or publication flags changed. Native success is not team
copy/design approval, proof of measured keyword demand, or permission to publish
or schedule. This proof does not certify unattended execution or ranking outcomes.
